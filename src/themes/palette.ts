import { StyleConstants } from "../common/styles/style-constants"
import { ExtendedPalette } from "./interfaces/extended-palette"

export const palette : ExtendedPalette = {
  dark: StyleConstants.color.dark,
  star: StyleConstants.color.gold
}